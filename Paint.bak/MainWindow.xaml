<Window x:Class="Paint.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Paint"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="Super Puper Paint In The World" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="200" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="LightGray">
            <Canvas x:Name="Canvas">
                
            </Canvas>
        </Grid>
        <Grid Grid.Column="1" Background="Blue" Opacity="0.5">
            <WrapPanel>
                <Button Click="CreateLine_Click">Создать</Button>
                <Grid Height="22" Width="55" Background="White">
                    <xctk:ColorPicker x:Name="ColorPicker" SelectedColor="Black" SelectedColorChanged="ClrPcker_SelectedColorChanged"/>
                </Grid>
                <Button Click="ClearLines_Click">⟳</Button>
            </WrapPanel>
        </Grid>
    </Grid>
</Window>
